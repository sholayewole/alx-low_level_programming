#include "main.h"
#include <stdio.h>

/**
 * main - program that prints its name
 *
 * @argc: agrument
 * @argv: argument
 * Return: 0
 */

int main(int argc, char *argv[]);
{
	int argsno = argc - 1;
	{
		printf("%d\n" argsno);
	}
	return (0);
}
