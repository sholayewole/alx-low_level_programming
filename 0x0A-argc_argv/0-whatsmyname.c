#include <stdlib.h>
#include <stdio.h>

/**
 * main - program that prints its name
 *
 * @argc: agrument
 * @argv: argument
 * Return: 0
 */

int main(int _attribute_((unused)) argc, char *argv[])
{
	int j = 0;

	printf("%s\n", argv[j]);
	return (0);
}
