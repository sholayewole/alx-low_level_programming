#include <stdio.h>

/**
 * main - program that prints its name
 *
 * @argc: agrument
 * @argv: argument
 * Return: 0
 */

int main(int argc, char *argv[]);
{
	char *myProgram = argv[0];
	{
	printf("%s\n", myProgram);
	}
	return (0);
}
